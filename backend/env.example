# Backend Environment Example

# Core
NODE_ENV=development
PORT=3001

# Auth
JWT_SECRET=wellspring_hospital_jwt_secret_2024
DEMO_AUTH_ALLOWED=false

# CORS
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:5173,http://localhost:8081,http://127.0.0.1:5173,http://127.0.0.1:8081,http://localhost:8083,https://wellspringhospitalbkc.netlify.app

# Firebase Admin
# Absolute path to service account JSON; if omitted will look for service-account.json in repo
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json
